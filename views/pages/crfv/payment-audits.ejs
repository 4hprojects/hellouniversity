<div class="app-container payment-audits-page">
  <div class="main-content">
    <div class="tabs-row payment-audits-title-row">
      <div class="payment-audits-title-copy">
        <h2>Payment Audits</h2>
        <p class="payment-audits-subtitle">Read-only CRFV payment reporting across all events.</p>
      </div>
      <a id="openPaymentReportsLink" class="export-btn" href="/crfv/payment-reports">
        Open Payment Reports
      </a>
    </div>

    <section class="search-section payment-audits-event-section">
      <div class="payment-audits-event-copy">
        <p class="payment-audits-section-eyebrow">Audit Scope</p>
        <h3 class="payment-audits-section-title">Review payment rows by event</h3>
        <p class="payment-audits-section-help">Inspect all CRFV payment records at once or narrow the audit to one event before exporting.</p>
      </div>
      <div class="payment-audits-event-controls">
        <div class="search-bar payment-audits-event-bar">
          <label for="eventFilter"><strong>Select Event:</strong></label>
          <select id="eventFilter">
            <option value="">All Events</option>
          </select>
        </div>
        <button id="clearEventFilterBtn" class="btn btn-secondary" type="button">Clear Filter</button>
      </div>
    </section>

    <div id="summaryStatus" class="payment-audits-status" aria-live="polite"></div>

    <section class="payment-audits-summary-section">
      <div class="payment-audits-summary-grid" id="summaryCards" aria-live="polite">
        <article class="payment-audit-summary-card skeleton-card"></article>
        <article class="payment-audit-summary-card skeleton-card"></article>
        <article class="payment-audit-summary-card skeleton-card"></article>
        <article class="payment-audit-summary-card skeleton-card"></article>
      </div>
      <div class="payment-audits-summary-stats" id="summaryStats" aria-live="polite">
        <article class="payment-audit-summary-stat skeleton-card"></article>
        <article class="payment-audit-summary-stat skeleton-card"></article>
        <article class="payment-audit-summary-stat skeleton-card"></article>
      </div>
    </section>

    <section class="search-section payment-audits-filter-section">
      <input type="text" id="paymentAuditSearch" placeholder="Search event, attendee, OR number, notes, and more...">
      <select id="paymentStatusFilter">
        <option value="">All Statuses</option>
      </select>
      <select id="recordsLimit" aria-label="Rows per page">
        <option value="25">25 Rows</option>
        <option value="50">50 Rows</option>
        <option value="100">100 Rows</option>
        <option value="250">250 Rows</option>
        <option value="all">All Rows</option>
      </select>
      <div id="columnPickerSection" class="payment-audits-column-picker">
        <button
          id="showColumnPickerBtn"
          class="btn btn-secondary payment-audits-column-trigger"
          type="button"
          aria-haspopup="menu"
          aria-controls="columnPickerDropdown"
        >
          <span class="payment-audits-column-trigger__label">Columns</span>
          <span id="columnPickerCount" class="payment-audits-column-trigger__count" aria-live="polite">9 visible</span>
          <span class="payment-audits-column-trigger__icon" aria-hidden="true">
            <i class="material-icons">expand_more</i>
          </span>
        </button>
        <div id="columnPickerDropdown" style="display:none;"></div>
      </div>
    </section>

    <div class="results-export-row payment-audits-meta-row">
      <div class="payment-audits-meta-copy">
        <div id="recordsResultsSummary"></div>
      </div>
      <div class="payment-audits-meta-actions">
        <button class="export-btn" id="exportAuditXLSXBtn" type="button">Export XLSX</button>
        <button class="export-btn" id="exportAuditPDFBtn" type="button">Export PDF</button>
        <div class="inline-pagination" id="inlineRecordsPagination"></div>
      </div>
    </div>

    <div class="table-container payment-audits-table-shell">
      <table id="paymentAuditRecordsTable">
        <thead>
          <tr id="paymentAuditRecordsHead">
            <th>Event</th>
            <th>Attendee No</th>
            <th>Attendee Name</th>
            <th>Organization</th>
            <th>Payment Status</th>
            <th>Amount</th>
            <th>Form of Payment</th>
            <th>OR Number</th>
            <th>Date Full Payment</th>
          </tr>
        </thead>
        <tbody id="paymentAuditRecordsBody">
          <tr>
            <td colspan="9" class="empty-state-row">Loading payment audit records...</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div id="recordsPagination" class="pagination"></div>
  </div>
</div>
